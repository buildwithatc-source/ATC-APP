node_modules
dist
out
.DS_Store
*.log
.env
.env.local
.env.*.local

# electron-builder output
release

# Capacitor
# dist-web = standalone web build (regenerated by build:web); ios not generated yet.
dist-web
ios
.capacitor
# NOTE: android/ IS committed — it carries native customizations (the landscape
# lock in AndroidManifest.xml). Its own android/.gitignore excludes build
# artifacts, copied web assets, and generated config, so only source is tracked.
